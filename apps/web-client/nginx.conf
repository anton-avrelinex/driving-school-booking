server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: Vue Router uses HTML5 history mode, so unknown paths
    # must serve index.html and let the client-side router resolve them.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
